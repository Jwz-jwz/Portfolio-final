export const HeadName = ({ text }) => {
  return (
    <div className="text-[14px] font-[500] leading-[20px] bg-[var(--head-bg)] text-[var(--text-color)] rounded-[12px] px-[20px] py-[4px]">
      {text}
    </div>
  );
};
